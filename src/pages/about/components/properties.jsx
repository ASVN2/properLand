import React from 'react';
import Navbar from '../../../sharedComponents/Navbar';

const properties = () => {
  return (
    <div>
      <Navbar image={'images/about.png'} title={'properties'} btnstate={false} headState={true} headtext={'properties'} />
    </div>
  );
};

export default properties;
